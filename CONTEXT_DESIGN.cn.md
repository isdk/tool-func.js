# ToolFunc 执行上下文与影子实例设计手册

本文件记录了 `@isdk/tool-func` 框架中关于**执行上下文 (Execution Context)**、**影子实例 (Shadow Instance)** 以及**原型链隔离机制**的核心设计原则与开发经验（避坑指南）。

---

## 1. 影子实例 (Shadow Instance) 设计哲学

### 为什么需要影子实例？
在并发环境下（如 Node.js 处理多个并发请求），工具函数（ToolFunc）实例是长生命周期的“单例/定义”，而执行上下文（`ctx`）是瞬时的“环境”。
*   **并发冲突**：如果直接在 ToolFunc 实例上修改 `this.ctx`，并发调用会互相覆盖。
*   **零拷贝隔离**：我们不使用昂贵的深拷贝，而是利用 Javascript 的原型链（`Object.create(this)`）创建一个轻量级的“影子”。
*   **逻辑共享**：影子实例通过原型链共享 Root 实例的定义（`name`, `params`, `func`），但拥有自己独立的 `this.ctx` 属性空间。

---

## 2. 原型链陷阱 (The Prototype Traps) —— 严禁“过度优化”

在维护 `_prepareContext` 等核心方法时，必须警惕以下两个常见的“伪优化”陷阱：

### ⚠️ 陷阱 A：使用 `Object.assign` 合并上下文
*   **现象**：尝试通过 `Object.assign({}, parentCtx, ctx)` 来快速创建新上下文。
*   **后果**：**属性丢失**。`Object.assign` 仅拷贝对象的“自有属性（Own Properties）”。在嵌套调用（如 `.with().with()` 或 `runAs`）中，父级的上下文数据存在于原型链上，使用 `assign` 会导致这些继承的数据被彻底丢弃。
*   **正确做法**：使用 `Object.create(parentCtx)` 建立原型链继承，确保整个上下文链条的可见性。

### ⚠️ 陷阱 B：使用 `Object.setPrototypeOf`
*   **现象**：为了在现有对象上挂载原型。
*   **后果**：**性能严重下降**。在 V8 引擎中，动态修改原型是“极其昂贵”的操作，会导致该对象在整个生命周期内无法被引擎优化。
*   **正确做法**：使用 `Object.assign(Object.create(proto), ctx)` 在创建时即确定原型。

---

## 3. 插件系统与 AOP 路径 (Static vs. Instance)

### 静态与实例方法的分工协作
在 `ToolFunc` 的架构设计中，`_prepareContext` 被拆分为静态（Static）和实例（Instance）两个版本，它们各自承担着不同的职责：

1. **静态 `Static._prepareContext`：标准合并引擎 (The "What")**
   * **职责**：它定义了处理上下文合并的**标准算法**（即 `Object.create` + `Object.assign` 逻辑）。
   * **作用**：它是整个框架处理原型链继承的“单一点真理”。无论是全局静态代理类（`ToolFunc.with()`）还是具体的实例调用，最终都会由它来执行底层的对象合并逻辑。
   * **可扩展性**：如果你想全局修改上下文的合并规则（例如改用深拷贝），只需重写这个静态方法。

2. **实例 `this._prepareContext`：AOP 扩展钩子 (The "When")**
   * **职责**：它定义了**何时**以及**如何**在特定调用中应用上下文，并负责**注入动态能力**。
   * **作用**：它是所有 AOP 插件（如 `CancelableAbility`）的**唯一切入点**。插件通过拦截这个实例方法，在调用静态引擎之前或之后，根据工具实例的特性（如是否开启了 `Cancelable`）注入特定的属性（如 `aborter`）。
   * **可扩展性**：它赋予了每个工具实例“按需增强”的能力。即便静态引擎逻辑不变，不同实例也可以根据自身状态拥有完全不同的上下文行为。

### 核心原则：严禁在内核中跳过实例钩子
* **背景**：`CancelableAbility` 等插件通过 AOP 拦截实例方法（如 `$_prepareContext`）来工作。
* **错误路径**：如果在 `with()` 或 `runAsSync()` 中直接调用静态 `ToolFunc._prepareContext`，会由于跳过了 `this` 路径而导致所有插件的拦截逻辑被完全绕过，从而引发 `aborter` 注入失效等严重 Bug。
* **正确做法**：内核逻辑必须始终通过 `this._prepareContext`（实例路径）进行调用。默认情况下，该实例方法会透明地委托给静态版本，但在安装了插件的环境中，它将自动承载起所有 AOP 拦截链条。

---

## 4. 并发状态的“根”追踪 (`_origin`)

### 持久状态与瞬时影子的分离
影子实例随调用产生，随调用销毁。因此，所有**持久化状态**（如并发控制的 `Semaphore`、正在运行的任务池 `__task_aborter`）**绝对不能**存储在影子实例上。

*   **锚定机制**：通过在构造函数中使用闭包 `get: () => this` 定义 `_origin` 属性。
*   **优势**：无论影子嵌套多少层，`this._origin` 始终指向那个最初被 `new` 出来的 Root 实例。
*   **测试兼容性**：为 `_origin` 提供一个“空 Setter”和 `configurable: true`，以防止 Vitest 等测试框架在失败进行对象 Diff 时因尝试写入只读属性而崩溃。

---

## 5. 影子隔离优先级决策树

`_shouldIsolate` 方法决定了何时需要创建一个新的影子实例。其优先级逻辑如下：

1.  **最高优先级**：调用时显式传入的 `ctx.isolated` 标志。
2.  **默认安全**：只要传入了任何 `ctx` 覆盖属性，必须隔离（以防污染原型）。
3.  **递归保护**：如果当前实例已经是影子（拥有自有属性 `ctx`），且无强制隔离要求，则复用现有实例以避免无限嵌套。
4.  **预置配置**：继承自原型链的 `this.ctx.isolated` 配置。
5.  **兜底逻辑**：如果存在任何有效的上下文，默认开启隔离以确保并发安全。

---

## 总结

**“理解对象在每一时刻的身份（Identity）和它的原型深度（Prototype Depth）”** 是维护这套架构的核心。任何破坏原型链继承或绕过实例路径的改动，都可能导致难以察觉的并发 Bug 或插件功能失效。
